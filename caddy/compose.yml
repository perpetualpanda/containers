services:
  caddy:
    image: caddy:latest
    container_name: caddy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
      - "5984:5984/tcp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - data:/data
      - config:/config
    networks:
      - proxy-network
    restart: unless-stopped

volumes:
  data:
  config:

networks:
  proxy-network:
    external: true
