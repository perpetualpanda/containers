enable: ${CFG_RCON_ENABLED}
host: 0.0.0.0
port: 25575
password: "${CFG_RCON_PASSWORD}"
colors: true
console-output: true
permissions:
  luck-perms:
    enable: false
    group: '*'
  regex:
    enable: false
    regex: minecraft\.(.*)
